FORMAT: IVRONE1
NAME: TestApp
DB: TestDb
SOUNDS: testsounddir

def main:
  play 'vwelcome' + (rand 1, 50)
  # Comment test

  $code = $obj_var.test

  if @is_mobile == 1
    if @is_alive
      goto mob_proc
    else
      goto fix_proc
    end
  else
    goto fix_proc
  end

def sample_proc:
  play 'sample_sound'
  $mobile = keycheck 'sample_sound', 10
  goto get_account


def get_account:
  $account = @db.find.account %'{"mobile": $mobile}'
  if $account == nil
    $account = @db.create.account %'{"mobile": $mobile, "tp": 0, "questions": []}'
  end
